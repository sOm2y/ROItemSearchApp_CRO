# 简体中文化 Backlog

目标：在不破坏现有繁中、英文、旧存档和在线数据更新的前提下，为项目提供完整的简体中文界面，并为后续 RO 游戏数据本地化建立稳定架构。

状态标记：`[ ]` 待处理、`[~]` 进行中、`[x]` 已完成、`[!]` 环境阻塞。

## P0：国际化基础设施

- [x] 将 `LangManager` 和 `tr()` 从 `ItemSearchApp.py` 抽离到共享 `i18n.py`
- [x] 支持 `zh_CN`、`zh_TW`、`en_US`，并限制非法语言代码
- [x] 从 `data/config.json` 读取语言，首次运行默认简体中文
- [x] 保持 `zh_CN -> zh_TW -> 默认文本/翻译键` 的回退链
- [x] 确保普通启动和 `rrf` 启动模式使用相同语言
- [~] 确保 PyInstaller/打包目录下仍能定位 `lang` 和 `data`

## P0：语言设置

- [x] 在偏好设置中增加语言下拉框
- [x] 将 `language` 保存到 `data/config.json`
- [x] 语言变化后提示重启
- [x] 不覆盖已有的更新模式、API Key 和界面缩放配置

## P0：语言包

- [x] 新增 `lang/zh_CN.json`
- [x] 补齐 `zh_TW.json` 当前缺少的翻译键
- [x] 补齐 `en_US.json` 当前缺少的翻译键
- [x] 保证三个语言包的格式变量一致
- [~] 建立 RO 简中术语表，避免仅做机械繁简转换

## P1：主程序界面

- [~] 清理 `ItemSearchApp.py` 中剩余的硬编码用户文案
- [~] 本地化窗口标题、菜单、页签、按钮、表头、占位文字、提示和错误消息
- [x] 本地化 `.ui` 文件中的存档管理器文案
- [~] 将内部稳定键与界面显示名称分离（装备部位已完成）
- [ ] 不修改旧存档使用的装备部位键、字段名和计算关键字

## P1：子工具

- [x] `RRF_compile_damage_view.py`：伤害解析器、表头、图表、状态和弹窗
- [x] `enchant.py`：附魔工具
- [x] `monster_lookup_dialog.py`：怪物查询
- [x] `skill_tree.py`：技能树
- [x] `Damage_view.py`：伤害历史、计算步骤名称及 ATK/MATK 状态切换
- [x] `reform_viewer.py`：改造工具
- [x] `packageitem.py`：箱子物品查询
- [x] `rrf_to_App.py`：转换错误和进度信息
- [x] `update.py`：独立更新器

## P1：质量检查

- [x] 检查语言包 JSON 重复键
- [x] 检查各语言包键集合一致
- [x] 检查 `{placeholder}` 集合一致
- [x] 检查 Python 中使用但语言包缺失的翻译键
- [x] 扫描新增的硬编码用户界面文案
- [x] 对所有 Python 文件执行语法检查
- [x] 执行不联网的 Qt 启动冒烟测试（PySide6 6.9.0，三语言与完整主窗口）
- [ ] 清理主窗口构造时的 `QLayout::addChildLayout` 重复父级警告

## P2：游戏内容简中化

- [~] 确定术语标准：当前以 OpenCC 繁转简为初稿，待按 cRO 客户端和社区确认译名校对
- [x] 按 Job ID 建立职业名称覆盖层，并兼容繁体、简体、英文名称及旧存档
- [x] 按 Skill ID 建立技能名称覆盖层，并兼容原名、简体名、技能代码及旧存档
- [~] 已按 Monster ID 覆盖本地 17 条预设/缓存怪物名称，并兼容多语言旧名称；待接入完整 CRO 怪物数据源
- [x] 按 Item ID 建立物品名称和说明覆盖层
- [x] 生成 `lang/items/zh_CN.json` 简体物品初稿
- [x] 增加 `zh_CN_overrides.json`，保护 CRO 人工校对结果不被重新生成覆盖
- [x] 增加 Divine Pride cRO 物品页名称校对工具和来源记录
- [x] 主程序、箱子查询、RRF 转换和改造工具接入物品覆盖层
- [x] 简体、繁体和原始资源名称同时进入搜索索引
- [x] 将装备效果、卡片和套装的关键名称反查改为稳定 Item ID 解析
- [x] 验证在线数据更新不会覆盖本地化覆盖层
- [ ] 按 cRO 客户端和社区确认译名人工校对装备、卡片、材料和消耗品名称
- [~] 装备效果解析器显示层已接入 `lang/effects/zh_CN.json`；继续分批校对术语，内部计算键保持不变

## 验收标准

- 三种语言均可启动并持久保存选择。
- 普通模式、RRF 模式和独立更新器使用一致语言。
- 主要工作流中不出现非预期繁中文字。
- 旧 JSON、ROC、RRF 文件可以正常加载和保存。
- 翻译只改变显示，不改变计算、映射、存档和导出使用的稳定值。
- 在线更新不会删除或覆盖简中语言资源。

## 最后处理：Divine Pride 全量物品校对

- [x] 已完成 20,708 个 Item ID 的全量扫描：9,626 条名称一致、9,720 条进入人工审核、1,361 条页面无有效名称，未扫描数量为 0。
- [x] 全量扫描已支持断点续传、跳过已校对条目、限速重试、批次限制、原子增量保存和失败报告。
- [x] 新增独立 `zh_CN_divine_pride.json` 校对层，加载优先级低于人工覆盖且不会被 OpenCC 生成器覆盖。
- [x] 已按人工确认规则采用 5,895 条简体中文候选，并录入 75 条等价或明确批准的 cRO 中文异体名称作为搜索别名；其余 3,750 条继续使用原名，其中 3,216 条不是可采用中文、534 条疑似韩文错误解码乱码，并分别输出采用/保留 CSV。
- [~] 默认只同步简体名称；说明文本仍需先生成差异报告，经人工确认后再批量写入。
